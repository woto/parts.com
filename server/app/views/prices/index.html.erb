<h1><%= t('Uploaded csv files') %></h1>

<p id="notice"><%= notice %></p>

<table>
  <tr>
    <th><%= t('Csv file') %></th>
    <th><%= t('.Created at') %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @prices.each do |price| %>
  <tr>
    <td><%= price.price_identifier %></td>
    <td><%= price.created_at.to_formatted_s(:short) %></td>
    <td><%= link_to controls('Download'), price %></td>
    <td><%= link_to controls('Destroy'), price, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t('Upload new csv file'), new_price_path %>
